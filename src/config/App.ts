import React from 'react';

export const CompanyName = 'Semesta';
export const ProjectName = 'Member Portal';

export const DefaultLayout = React.lazy(() => import('../components/containers/DefaultLayout'));
